import 'package:flutter/material.dart';

class ProtocolPageBody extends StatelessWidget {
  const ProtocolPageBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('This is the Protocol Page Body'),
    );
  }
}